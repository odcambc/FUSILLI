name: fusilli-qc
channels:
  - bioconda
  - conda-forge
  - nodefaults
dependencies:
  - python=3.11
  - fastqc
  - multiqc
  - snakemake-wrapper-utils>=0.6
  - pip
  - pip:
    # Install FUSILLI custom MultiQC modules from Git repository
    # This registers them as MultiQC plugins via setuptools entry points
    # TODO: Update with actual Git repository URL once published
    # For production use, install from PyPI: fusilli-multiqc>=1.0.0
    - git+https://github.com/user/fusilli-multiqc.git@main
